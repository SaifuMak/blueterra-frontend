import JournalClient from "@/components/clients/JournalClient"

export const metadata = {
    title: "Journals | Travel Stories & Adventures",
    description: "Explore travel journals, stories, and adventures from around the world."
};

export default function Journal() {

    return <JournalClient />
}