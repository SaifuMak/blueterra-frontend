import Image from "next/image"

export default function MobileAnimatedVerticalCard({ card, onClick, isExpanded, isFullCardVisible, handleHideFullCard, setIsLoading, isLoading, setIsFilterVisible }) {
   return (
      <div className=" w-full flex flex-col ">
         <div className="h-8 bg-red-200"></div>
        
      </div>
   )
}